{% block stylesheets %}
    {{ encore_entry_link_tags('modal_css') }}
{% endblock %}

{% block body %}
    <img class="garbage" src="{{ asset('images/poubelle.png') }}">
    <img id="photo" data-photo-id="{{ photo.id }}" src="{{ asset(photo.filePath) }}">
{% endblock %}

{% block javascripts %}
{% endblock %}

